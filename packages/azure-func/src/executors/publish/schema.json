{
	"$schema": "http://json-schema.org/schema",
	"version": 2,
	"title": "Publish executor",
	"description": "",
	"type": "object",
	"properties": {
		"azureAppName": {
			"type": "string",
			"description": "Name of the remote Azure Function App on Azure. If not set, application name will be used."
		},
		"buildTarget": {
			"type": "string",
			"description": "The build target used to resolve outputPath for publishing."
		},
		"buildTargetOptions": {
			"type": "object",
			"description": "Additional options used when resolving build target options.",
			"default": {}
		}
	},
	"additionalProperties": false,
	"required": ["buildTarget"]
}
